apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "common.fullname" . }}-cluster-role
  labels:
  {{- include "common.labels" . | nindent 4 }}
rules:
- apiGroups:
  - ""
  resourceNames:
  - scope
  resources:
  - configmaps
  verbs:
  - get
  - patch
  - put
  - update
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - create
