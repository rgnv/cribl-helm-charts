---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.fullname" . }}-internal
  labels:
    {{- include "common.labels" . | nindent 4 }}
  annotations:
    {{- include "common.service.annotations" (merge (dict "templateType" "internal") .) | nindent 4 }}
spec:
  type: {{ .Values.service.internalType }}
  ports:
      {{- range .Values.service.ports }}
      - port: {{ .port }}
        targetPort: {{ .port }}
        protocol: {{ .protocol }}
        name: {{ .name }}
        {{ if (and (eq $.Values.service.internalType "NodePort") (not (empty .nodePort))) }}
        nodePort: {{.nodePort}}
        {{ end }}
      {{- end }}
  selector:
    {{- include "common.selectorLabels" . | nindent 4 }}
  {{- if (and (not (empty .Values.service.internalLoadBalancerIP)) (eq $.Values.service.internalType "LoadBalancer"))  }}
  loadBalancerIP: {{ .Values.service.internalLoadBalancerIP }}
  {{- end -}}

